<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.advert.vidceramah">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YourActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".ustadSatu" />
        <activity android:name=".ustadDua" />
        <activity android:name=".ustadTiga" />
        <activity android:name=".ustadEmpat" />
        <activity android:name=".ustadLima" />
        <activity android:name=".vdplay"></activity>
    </application>

</manifest>